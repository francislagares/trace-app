import { NextRouter } from 'next/router';

export const mockedRouter: Partial<NextRouter> = {
  // Define any necessary properties or methods used by useRouter
  // For instance, if `query` is used in your component:
  query: {},
  // You might need to add other properties/methods your component uses
};

export default mockedRouter;
